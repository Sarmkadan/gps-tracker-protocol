# ILoggingPipeline

The `ILoggingPipeline` interface defines a contract for logging operations related to GPS tracker protocol processing, including frame reception, parsing, and storage events. It provides contextual logging capabilities with correlation tracking for individual processing flows.

## API

### `LoggingPipeline`
